         name          
-----------------------
 Investment
 Other Income
 Rent
 Utilities
 Home Insurance
 Property Tax
 Car Maintenance
 Car Loan
:/// missing these categories

replaced this: 
   
                            {/* <select className="form-select"
                                    value={categoryId}
                                    onChange={
                                        (e) => setCategoryId(e.target.value)
                                    }
                               >
                                <option value="">Select category</option>
                                {filteredCategories.map((c) => (
                                    
                                <option key={c.id} value={c.id} >
                                    {c.name}
                                </option>
                                ))}
                            </select> */}
with CategoryDropdown.tsx